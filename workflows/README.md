# Workflows

Store ComfyUI API-format workflow JSON files here when they are safe to publish. Never commit private credentials, access tokens, cookies, or machine-specific secrets.

Recommended starting workflow: the official Wan 2.2 5B video-generation template from current ComfyUI documentation.
