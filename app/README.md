# Realizer web app

This directory contains the browser gateway. It is designed to remain lightweight so it can run on low-power devices while AI generation occurs on a remote backend or capable local GPU machine.
